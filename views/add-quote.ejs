<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add Quote</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <nav>
    <h1>Quote Collection</h1>
    <ul>
      <li><a href="/">Home</a></li>
      <li><a href="/quotes">Browse Quotes</a></li>
      <li><a href="/add-quote">Add Quote</a></li>
    </ul>
  </nav>

  <div class="container">
    <h2>Add a New Quote</h2>
    
    <% if (typeof message !== 'undefined' && message) { %>
      <div class="message <%= messageType %>">
        <%= message %>
      </div>
    <% } %>

    <form action="/add-quote" method="POST" class="quote-form">
      <div class="form-group">
        <label for="text">Quote Text: *</label>
        <textarea id="text" name="text" rows="4" required 
          placeholder="Enter the inspirational quote here..."></textarea>
      </div>

      <div class="form-group">
        <label for="authorId">Author: *</label>
        <select id="authorId" name="authorId" required>
          <option value="">Select an author</option>
          <% authors.forEach(author => { %>
            <option value="<%= author.id %>"><%= author.name %></option>
          <% }) %>
        </select>
      </div>

      <div class="form-group">
        <label for="categoryId">Category: *</label>
        <select id="categoryId" name="categoryId" required>
          <option value="">Select a category</option>
          <% categories.forEach(category => { %>
            <option value="<%= category.id %>"><%= category.name %></option>
          <% }) %>
        </select>
      </div>

      <div class="form-row">
        <div class="form-group">
          <label for="year">Year (optional):</label>
          <input type="number" id="year" name="year" min="1" max="2024" 
            placeholder="e.g., 1955">
        </div>

        <div class="form-group">
          <label for="likes">Initial Likes (optional):</label>
          <input type="number" id="likes" name="likes" min="0" value="0">
        </div>
      </div>

      <button type="submit" class="btn-primary">Add Quote</button>
      <a href="/quotes" class="btn-secondary">Cancel</a>
    </form>

    <div class="info-section">
      <h3>Tips</h3>
      <ul>
        <li>Make sure the quote is accurate and properly attributed</li>
        <li>Choose the most relevant category for the quote</li>
        <li>Add the year if you know when it was said or written</li>
      </ul>
    </div>
  </div>

  <footer>
    <p>SBA 318 - Express Server Application | Quote Collection</p>
  </footer>
</body>
</html>
